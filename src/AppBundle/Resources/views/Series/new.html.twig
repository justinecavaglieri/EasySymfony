{% extends '::layout.html.twig' %}

{% block content -%}
    <h1>Series creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('series') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
